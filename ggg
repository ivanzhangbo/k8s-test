cat > /etc/nginx/nginx.conf <<'EOF'
events {}

http {
    log_format header_debug
        'remote_addr="$remote_addr" '
        'request="$request" '
        'status=$status '
        'abc="$http_abc" '
        'xff="$http_x_forwarded_for" '
        'host="$host" '
        'ua="$http_user_agent"';

    access_log /dev/stdout header_debug;
    error_log /dev/stderr warn;

    server {
        listen 80;

        location /debug-abc {
            default_type text/plain;
            return 200 "abc=$http_abc\nxff=$http_x_forwarded_for\nhost=$host\n";
        }

        location / {
            return 200 "ok\n";
        }
    }
}
EOF


https://docs.cloud.google.com/load-balancing/docs/tcp/set-up-ext-reg-tcp-proxy-hybrid?hl=ja
https://docs.cloud.google.com/load-balancing/docs/tcp/set-up-ext-reg-tcp-proxy-internet?hl=ja
echo 'add_header Set-Cookie "my_cookie=my_value; Path=/;";' >> /etc/nginx/nginx.conf
curl -x http://127.0.0.1:7890 -X POST -H "Content-Type: application/json" -d "{\"name\":\"test\",\"age\":18}" https://example.com/api
https://docs.cloud.google.com/managed-lustre/docs/performance?utm_source=chatgpt.com&hl=ja#gke



https://zenn.dev/yoshida12/articles/62b44dad6d73b9
https://docs.cloud.google.com/load-balancing/docs/https/troubleshooting-ext-https-lbs?hl=ja
kubectl get pods -n <namespace> -o jsonpath='{range .items[*]}{.metadata.name}{"\t"}{range .spec.volumes[*]}{.persistentVolumeClaim.claimName}{" "}{end}{"\n"}{end}' | grep PVC-NS-001
