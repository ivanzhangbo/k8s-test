nginx keepalive_timeout fixes the normal idle connection reuse mismatch.
terminationGracePeriodSeconds and drainingTimeoutSec fix graceful shutdown / endpoint removal issues.
They are related to 503 investigation, but they are not mandatory prerequisites for changing nginx keepalive_timeout.
